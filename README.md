# Clon académico — Programa de Ingeniería de Software (UCompensar)

> ⚠️ Este proyecto es un **ejercicio educativo** para la asignatura. No es el sitio oficial.

## Cómo ejecutar
1. Descarga este paquete y descomprímelo.
2. Abre `index.html` en tu navegador.
3. (Opcional) Sirve con un HTTP server local para evitar problemas de CORS.

## Estructura
- `index.html`: Maquetación HTML semántica y metadatos (`meta charset`, `viewport`, `description`, `robots`).  
- `styles.css`: **CSS externo**: layout, tipografía, temas claro/oscuro, componentes (botón, card, grid).  
- `script.js`: **JavaScript** para modificar estructura y estilos: menú accesible con ARIA, validación de formulario, inserción dinámica de nodos, cambio de tema.

## Criterios cubiertos
- **Asociación con funcionalidades del navegador**: meta viewport responsive, preloads, navegación con hash (`#`), `details/summary`, validación nativa `required`, atributos accesibles `aria-*`.
- **Controles de acceso y datos**: ejemplo de formulario con validación básica (cliente). Sugerencia: complementar con validación del lado servidor en un curso posterior.
- **Buenas prácticas**: HTML semántico (`header`, `main`, `section`, `nav`), CSS externo, accesibilidad (skip link, contrastes, focus visible), SEO básico (title + meta description).

## Licencia
Uso académico.

## Referencias sugeridas (para el documento APA)
- Mozilla Developer Network (MDN). *<meta>: The metadata element*. https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/meta
- Google Search Central. *How to Write Meta Descriptions*. https://developers.google.com/search/docs/appearance/snippet
- MDN. *<em> element* y *Viewport meta element*. https://developer.mozilla.org/
- W3C. *WCAG 2.2* y *HTML Living Standard (em/strong)*. https://www.w3.org/
